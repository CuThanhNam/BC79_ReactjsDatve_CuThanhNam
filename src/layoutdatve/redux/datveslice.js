import { createSlice } from '@reduxjs/toolkit'

const initialState = {

}

const datveslice = createSlice({
  name: second,
  initialState,
  reducers: {}
});

export const {} = datveslice.actions

export default datveslice.reducer